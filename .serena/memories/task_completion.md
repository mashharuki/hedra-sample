# Task completion

For changes in `tutorial-local-hardhat/`, run `npm run build` and `npm test`. Run a targeted `npx hardhat test solidity` or `npx hardhat test nodejs` while iterating. Do not run `deploy` or `send-tx` as a routine check: they require configured Hedera credentials and may broadcast transactions.