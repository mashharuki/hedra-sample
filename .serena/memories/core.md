# Core

- Two independent runnable projects:
  - `tutorial-local-hardhat/`: Hardhat + viem Hedera contract tutorial. Read `mem:hardhat`.
  - `x402-sample/`: pnpm workspace demonstrating a Hedera Testnet x402 v2 paid-resource flow. Read `mem:x402_sample`.
- Root `README.md` is Japanese Hedera research/learning material with links and adoption guidance.
- Cross-project conventions: `mem:conventions`; verification and network-safety rules: `mem:task_completion`.